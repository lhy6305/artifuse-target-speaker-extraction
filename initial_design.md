# 目标说话人提取前置模块（TSE-Prefix）设计稿 V1.0

> **版本号**：V1.0
> **版本定位**：独立并行子项目 / Source 前置净化模块设计稿
> **目标**：从混合录音中仅保留指定目标说话人（当前为用户本人）的语音，抑制其他说话人、背景噪声、音乐和环境干扰，作为后续变声系统的前置输入净化模块。
> **核心原则**：
> 1. 本项目与现有变声项目**完全独立维护**，目录、代码、实验、配置、数据清单与报告均单独管理；
> 2. 第一阶段不尝试与现有 VC 主模型联合训练，优先采用**前置独立模型串联**；
> 3. 任务定义优先采用 **Target Speaker Extraction（目标说话人提取）**，而非泛化的“人声分离”；
> 4. 第一阶段优先用**合成混合数据**启动，而不是等待大量真实多说话人标注录音；
> 5. 现有 source 数据可作为 pseudo-clean target speech 使用，但必须显式防范“固定底噪被模型误学为身份特征”的风险；
> 6. 第一阶段优先追求“是否可用”和“是否值得继续”的工程可行性结论，而非一开始追求最优学术指标。

---

# 目录

1. 版本摘要 [<sup>1</sup>](#版本摘要)
2. 项目目标与非目标 [<sup>2</sup>](#项目目标与非目标)
3. 术语与符号 [<sup>3</sup>](#术语与符号)
4. 总体设计原则 [<sup>4</sup>](#总体设计原则)
5. 总体架构 [<sup>5</sup>](#总体架构)
6. 与现有变声项目的边界关系 [<sup>6</sup>](#与现有变声项目的边界关系)
7. 任务定义与问题分解 [<sup>7</sup>](#任务定义与问题分解)
8. 数据来源与数据分桶设计 [<sup>8</sup>](#数据来源与数据分桶设计)
9. 合成训练数据生成策略 [<sup>9</sup>](#合成训练数据生成策略)
10. 模型路线候选 [<sup>10</sup>](#模型路线候选)
11. 推荐 MVP 路线 [<sup>11</sup>](#推荐-mvp-路线)
12. 训练目标与损失设计 [<sup>12</sup>](#训练目标与损失设计)
13. 评估指标与验收标准 [<sup>13</sup>](#评估指标与验收标准)
14. 如何接入现有变声系统 [<sup>14</sup>](#如何接入现有变声系统)
15. 风险、边界与退化策略 [<sup>15</sup>](#风险边界与退化策略)
16. 推荐实施路线图 [<sup>16</sup>](#推荐实施路线图)
17. 附录：MVP 最小可实施版本 [<sup>17</sup>](#附录mvp-最小可实施版本)

---

# 版本摘要

## 相比“泛人声分离”思路的关键收敛

本项目不以“分出所有人声”为目标，而是明确收敛为：

> **给定目标说话人参考音频，仅保留目标说话人的语音输出。**

因此本项目属于：

- **Target Speaker Extraction (TSE)**
- 或 **Target Speech Extraction / Personalized Speech Extraction**

而不优先采用以下任务定义：

- 泛人声分离（把所有人声全部拆开）
- 纯降噪（只去环境噪声）
- 纯语音增强（不区分说话人身份）

---

## 本项目的工程定位

该模块在当前系统阶段中的推荐位置为：

```text
原始输入录音
-> 目标说话人提取模块（TSE-Prefix）
-> 更干净的 source 音频
-> 现有变声模型（VC 主线）
-> 目标说话人音色输出
```

V1.0 明确规定：

- **先做前置独立模型**
- **不直接并入现有 VC 主模型**
- 后续若证明长期有价值，再评估共享前端或联合蒸馏

---

## V1.0 的数据策略

V1.0 不等待“完美真实录音数据”到位，而是采用：

- 用户本人现有 source 切片，作为 **pseudo-clean target speech**
- 高质量游戏语音集，作为 **干净 speech interference**
- 歌曲与其 UVR 处理产物，作为 **music / singing interference**
- 同性别同学录音，作为 **hard negative / domain-matched speech interference**

通过可控的波形级混合，快速构造 TSE 训练与验证数据。

---

# 项目目标与非目标

## 1. 项目目标

本项目当前阶段目标为：

### 1.1 目标说话人保留
在输入混合音频中，仅保留指定目标说话人（当前为用户本人）的语音内容。

### 1.2 干扰抑制
尽量压制以下干扰：
- 其他说话人的语音
- 环境背景噪声
- 办公噪声
- 音乐 / 伴奏
- 非目标歌声或其他人声音源

### 1.3 与后续 VC 链路兼容
输出音频应满足：
- 不明显损伤内容可懂度
- 不引入过强伪影
- 保留足够的语音边界、停顿、能量结构
- 适合作为现有变声模型的输入 source

### 1.4 独立可评估
该模块必须可在不依赖 VC 主系统的情况下单独评估：
- 是否保留了用户本人声音
- 是否压掉了其他人声音
- 是否降低了背景干扰
- 是否过度伤害语音自然性

---

## 2. 非目标

### 2.1 不追求“分出所有说话人”
本项目不以盲源分离为目标，不要求把每个说话人分别输出。

### 2.2 不承诺首版就完全去掉所有噪声
首版优先追求：
- 保留目标人
- 压掉主要竞争说话人
- 尽量改善背景
而不是保证极致干净

### 2.3 不承诺首版直接并入 VC 主模型
当前阶段不做：
- TSE 与 VC 的统一端到端大模型
- 单模型同时完成提取与变声

### 2.4 不承诺 pseudo-clean target 完全无污染
首版允许目标语音中存在：
- 轻微规律底噪
- 轻度环境底噪
但必须显式监控其是否被模型误学为身份 cue

---

# 术语与符号

| 符号 / 术语 | 含义 |
|---|---|
| `s_target(t)` | 目标说话人语音（当前为用户本人） |
| `s_ref(t)` | 目标说话人参考音频 / enrollment audio |
| `s_intf(t)` | 干扰说话人语音 |
| `m_music(t)` | 音乐或伴奏干扰 |
| `n_env(t)` | 环境噪声 / 自然噪声 / 办公噪声 |
| `x_mix(t)` | 最终混合输入波形 |
| `y_hat(t)` | 模型输出的目标说话人提取结果 |
| TSE | Target Speaker Extraction，目标说话人提取 |
| Enrollment / Reference | 目标说话人参考音频，用于告诉模型“我要保留的是谁” |
| Pseudo-clean target | 近似干净目标语音，不是实验室级 clean，但足够作为训练目标 |
| Hard negative | 难干扰样本，通常与目标说话人更相似或更接近真实域 |
| Domain-matched interference | 与真实 source 场景更接近的干扰语音 |
| RIR | Room Impulse Response，房间脉冲响应，用于模拟混响 |

---

# 总体设计原则

## 原则 1：任务定义必须明确为“只保留目标说话人”
本项目的目标不是：
- 全说话人分离
- 全背景净化
而是：
- 在混合中保留指定目标说话人
- 其他一切都是应被抑制的干扰

---

## 原则 2：首阶段优先使用独立前置模型
本项目首阶段输出的是：

> 更干净、更单人的 source 音频

而不是直接输出最终变声音频。

因此优先链路为：

```text
TSE-Prefix -> VC 主模型
```

---

## 原则 3：首阶段优先依赖合成数据，不等待真实标注混合录音
由于真实“目标人 + 干扰人 + 环境”全标注数据难获得，首阶段采用：
- 目标语音
- 干扰语音
- 背景噪声
- 音乐
通过规则合成混合样本

---

## 原则 4：必须显式防范“固定底噪误当身份特征”
用户本人现有 source 数据可能存在：
- 规律底噪
- 固定白噪
- 少量环境纹理

这些可被暂时接受，但不能让模型把它们学成：
- target speaker identity cue

---

## 原则 5：先验证可用性，再决定是否扩大训练复杂度
V1.0 的首要目标不是追求论文级领先指标，而是回答：

1. 这条路线在用户当前数据条件下是否有效？
2. 输出是否值得接到现有 VC 项目前面？
3. 是否值得继续投入更复杂的数据和训练成本？

---

# 总体架构

## 1. 训练期总体结构

```text
目标说话人语音池（用户本人）
    +
目标参考音频池（用户本人 enrollment）
    +
干扰语音池（游戏语音 / 同学录音）
    +
音乐 / 歌声 / 环境噪声池
    ↓
混合样本生成器
    ↓
(x_mix, s_ref, s_target_clean)
    ↓
TSE 模型
    ↓
y_hat
    ↓
重建损失 + 说话人保持约束 + 干扰抑制评估
```

---

## 2. 推理期总体结构

```text
原始输入录音
    +
目标说话人参考音频
    ↓
TSE 模型
    ↓
更干净的目标说话人单人音频
    ↓
（可选）接入现有 VC 主模型
```

---

# 与现有变声项目的边界关系

## 1. 项目边界

本项目与现有 VC 项目必须满足以下边界：

### 独立维护
- 独立仓库或独立顶级目录
- 独立 `configs/`
- 独立 `src/`
- 独立 `reports/`
- 独立 `docs/`
- 独立 `experiments/`

### 独立实验记录
- 不共用现有 VC 的 route / anchor / handoff 体系
- 不混入 VC 的实验编号与 metrics 主文件

### 允许的数据引用边界
可以引用现有 VC 项目的**输出产物**，例如：
- 已清洗的 source 切片
- 已确认可用的 target/source manifest
- 人耳试听过滤后的样本集

但引用应视作：
- 外部输入数据
- 而非共享内部状态

---

## 2. 与 VC 主系统的关系

当前阶段，本模块在系统中的推荐定位是：

- **前置净化模块**
- 不参与现有 VC 主训练闭环
- 不改变现有 VC 的监督定义、route、anchor 和 special eval

---

## 3. 未来可能的演进路线

只有在以下条件同时满足时，才考虑更深融合：
1. TSE-Prefix 长期稳定有效；
2. 真实用户场景中确实频繁存在多说话人或复杂背景；
3. 串联延迟或算力成本已成为明显瓶颈；
4. 已经证明 TSE 输出对 VC 有稳定增益，而不仅是个别样本收益。

在此之前，不进入：
- shared backbone
- joint end-to-end training
- 单模型统一提取+变声

---

# 任务定义与问题分解

## 1. 主任务定义

给定：
- 混合音频 `x_mix(t)`
- 目标说话人参考音频 `s_ref(t)`

预测：
- 目标说话人提取结果 `y_hat(t)`

希望：
\[
y_hat(t) \approx s_target(t)
\]

其中：
\[
x_{mix}(t) = s_{target}(t) + s_{intf}(t) + m_{music}(t) + n_{env}(t)
\]

---

## 2. 子问题拆分

### 2.1 目标说话人保真
输出中应尽量保留：
- 内容
- 时序边界
- 停顿
- 语速
- 可懂度

### 2.2 干扰压制
输出中应压制：
- 其他说话人
- 非目标歌声
- 音乐背景
- 环境底噪
- 办公噪声

### 2.3 目标人缺席处理
模型应尽量学会：
- 若某时段目标说话人不存在，则输出不应凭空生成语音
- 对于 target absent 片段，应保持抑制态

### 2.4 与 VC 接口兼容
输出不应出现：
- 大量 musical noise
- 语音边界被严重平滑
- 爆破 / 辅音完全糊掉
- 极重的相位伪影

---

# 数据来源与数据分桶设计

## 1. 现有可用数据来源

### A. 用户本人 source 切片
特点：
- 已切片
- 已过人耳试听过滤
- 可能带规律底噪
- 可能有少量残余瞬态
- 相对干净，可作为 pseudo-clean target

### B. 游戏语音集（高质量单说话人）
特点：
- 单人
- 干净
- 语音质量高
- 适合做 clean speech interference

### C. 各类歌曲及其 UVR 处理结果
特点：
- 可拆出 accompaniment / vocal
- 适合分别构造成音乐干扰与歌声干扰

### D. 同性别同学录音
特点：
- 与用户 source 场景接近
- 可能带底噪或轻微瞬态
- 可能夹杂少量其他人声
- 适合做 hard negative / domain-matched interference

---

## 2. 正式数据分桶

### 2.1 `target_speech_pool`
来源：
- 用户本人 source 切片

作用：
- 训练时的 target speech
- 评估时的 ground-truth target

要求：
- 尽量保持相对干净
- 不额外引入明显异常样本

---

### 2.2 `target_reference_pool`
来源：
- 用户本人 source 切片中更稳定、更干净的一部分
- 后续可独立补录更高质量 enrollment

作用：
- 参考音频池
- 用于提供 target identity clue

要求：
- 与 `target_speech_pool` 分开抽样
- 避免同一语句直接同时当 target 与 reference

---

### 2.3 `speech_interference_clean_pool`
来源：
- 游戏语音集

作用：
- 标准 speaking interference

---

### 2.4 `speech_interference_hard_pool`
来源：
- 同性别同学录音

作用：
- 更困难、更接近真实域的人声干扰

---

### 2.5 `music_interference_pool`
来源：
- UVR 去人声后的 accompaniment

作用：
- 音乐背景干扰

---

### 2.6 `singing_vocal_interference_pool`
来源：
- UVR 仅留人声的歌曲 vocal

作用：
- 歌声干扰
- 单独作为特殊 bucket 控制比例

---

### 2.7 `ambient_noise_pool`
来源：
- 自然环境音、办公噪声、生活噪声（若后续补充）

作用：
- 非语音背景干扰

---

# 合成训练数据生成策略

## 1. 合成目标

合成器应生成以下最小字段：

- `mixture_audio_path`
- `target_audio_path`
- `reference_audio_path`
- `metadata.json`

其中 `metadata` 至少包含：
- `interference_types`
- `snr_or_gain_setup`
- `target_present_ratio`
- `reference_source_id`
- `target_source_id`
- `hard_negative_used`
- `music_used`
- `singing_used`
- `noise_used`

---

## 2. 基本混合形式

### 2.1 波形混合
\[
x_{mix}(t) = s_{target}(t) + \alpha s_{intf}(t) + \beta m_{music}(t) + \gamma n_{env}(t)
\]

### 2.2 归一与限制
混合前后必须控制：
- 峰值
- RMS / LUFS
- clipping 防护

### 2.3 不允许的错误混法
- 不做响度控制直接相加
- 不做长度对齐直接截断
- 不控制爆音
- 永远固定相同 gain 组合

---

## 3. 混合难度设计

### 3.1 目标/干扰比
建议至少覆盖多个档位：
- 容易样本：目标明显更强
- 中等样本：目标与干扰接近
- 困难样本：干扰略强于目标

不要只保留单一固定比值。

---

### 3.2 时间结构设计
必须包含：
- `target-only`
- `interference-only`
- `partial overlap`
- `heavy overlap`
- `target intermittent`
- `target absent tail`

不建议全部样本都做“全程完全重叠”。

---

### 3.3 干扰组合类型
建议至少覆盖：
1. `target + clean_speech`
2. `target + hard_speech`
3. `target + music`
4. `target + clean_speech + noise`
5. `target + hard_speech + music`
6. 少量 `target + singing_vocal`

---

## 4. 合成 realism 增强（可选）
在非 MVP 阶段，可逐步加入：
- 轻混响
- RIR 卷积
- 设备频响差异
- 压缩 / 码率损伤

V1.0 MVP 不要求一开始全部加入。

---

# 模型路线候选

## 1. 路线 A：现成 TSE baseline 直接验证
思路：
- 选成熟 target speaker extraction baseline
- 用用户 reference 音频做条件输入
- 直接对合成集和少量真实样本跑 inference

优点：
- 启动快
- 可先回答“方向是否可用”

缺点：
- 对当前域的适配可能有限

---

## 2. 路线 B：预训练模型 + 轻适配
思路：
- 在公开预训练 TSE 模型上做轻量微调或适配

优点：
- 比从零训练更稳
- 更可能快速得到可用结果

缺点：
- 依赖可获得、可用的现成权重与实现

---

## 3. 路线 C：从零训练轻量 TSE 模型
思路：
- 从合成数据开始训练一个独立 TSE 模型

优点：
- 完全可控
- 更容易针对当前场景裁剪

缺点：
- 训练与调参成本更高
- 对数据生成质量要求更高

---

# 推荐 MVP 路线

## 当前推荐：A/B 优先，C 作为后备
V1.0 的推荐顺序为：

### Phase 1
- 先用现成 baseline 验证任务可行性

### Phase 2
- 若可行，再考虑预训练模型轻适配

### Phase 3
- 若公开路线不能满足场景要求，再进入自训轻量 TSE

---

## 当前不推荐的路线
不建议一开始就：
- 做大规模自训复杂 TSE
- 做 TSE+VC 一体化端到端
- 同时引入过多 realism 增强变量

---

# 训练目标与损失设计

## 1. 主重建目标
目标：
\[
y_hat(t) \approx s_{target}(t)
\]

### 候选损失
- 波形 L1 / SI-SDR
- 频谱重建损失
- 多分辨率 STFT 损失

---

## 2. 目标说话人保持约束
确保输出中保留的是目标人，而不是别人的语音。

可采用：
- speaker embedding similarity
- target-vs-interference contrastive objective（后续阶段）

---

## 3. 干扰抑制评估目标
虽然不一定全部显式写进 loss，但训练期和验证期应监控：
- 对其他说话人的抑制度
- 对背景音乐的抑制度
- 对环境噪声的抑制度

---

## 4. 目标缺席约束（后续增强项）
若某时段目标说话人不存在：
- 输出应趋于抑制态
- 不应 hallucinate 出目标语音

---

# 评估指标与验收标准

## 1. 提取保真
### 自动指标
- SI-SDR / SI-SDRi
- SDR / SDRi
- STOI
- PESQ（若适用）

### 听感关注点
- 目标内容是否保住
- 是否明显糊掉辅音
- 是否出现 musical noise
- 是否边界被过度平滑

---

## 2. 目标说话人保留
可使用：
- speaker embedding cosine similarity
- 用户主观 AB 判断

关注：
- 输出是否仍然像“用户本人”
- 是否把干扰人声带进来

---

## 3. 干扰抑制
重点主观评估：
- 别人说话是否明显减弱
- 背景音乐是否明显压低
- 环境底噪是否改善

---

## 4. 作为 VC 前置的可用性验收
这是本项目最重要的业务验收：

### 最低通过标准
当 `TSE output -> VC input` 时：
- 不应比原始 source 直接进 VC 更差很多
- 在含干扰场景下应优于原始 source 直接进 VC
- 不应显著破坏内容与节奏

---

# 如何接入现有变声系统

## 1. 当前推荐接法：前置串联
```text
raw input
-> TSE-Prefix
-> cleaned source
-> existing VC pipeline
```

---

## 2. 当前不推荐接法：直接模型合并
当前不建议：
- 共享训练
- 共享 route 体系
- 一体化单模型推理

原因：
- 任务目标不同
- 调试难度高
- 当前主 VC 项目已有独立核心瓶颈，不应被新任务干扰

---

## 3. 用户端产品形态建议

### 普通模式
- 输入较干净、单人讲话
- 直接走 VC

### 复杂环境模式
- 有他人语音 / 音乐 / 明显背景
- 先走 TSE，再进 VC

### 自动判断模式（远期）
- 系统先判定是否需要 TSE
- 再决定是否启用前置模块

---

# 风险、边界与退化策略

## 风险 1：固定底噪被学成 target identity cue
### 现象
- 模型保留某种固定噪声纹理
- 输出中把噪声也当成“用户本人”的一部分

### 对策
- reference 池与 target 池分开
- 干扰多样化
- 后续补更干净 enrollment
- 验证时加入不同噪声条件

---

## 风险 2：过度压制导致语音损伤
### 现象
- 内容可懂度下降
- 辅音边界糊掉
- 停顿结构异常

### 对策
- 保留 content-side 评估
- 不能只看“干净程度”
- 必须同时听 `TSE -> VC` 的最终链路效果

---

## 风险 3：合成数据与真实域差距过大
### 现象
- 合成集上效果好
- 真实办公录音上不稳定

### 对策
- 引入 domain-matched interference（同学录音）
- 引入 target absent / intermittent overlap
- 逐步加入轻混响与真实噪声

---

## 风险 4：歌曲 vocal 作为干扰引入奇怪分布
### 对策
- 将 singing vocal 单独分桶
- 控制比例
- 先不要与普通 speech interference 混同处理

---

# 推荐实施路线图

## Phase A：数据与 baseline 验证
目标：
- 建立合成数据管线
- 在少量样本上验证 baseline 是否可用

里程碑：
1. 完成 target/reference/interference 数据分桶
2. 生成首批合成训练/验证样本
3. 跑通 baseline inference 或最小训练闭环

---

## Phase B：前置模块可用性验证
目标：
- 确认 TSE 输出是否能作为 VC 前置输入

里程碑：
1. 单独 TSE 评估可接受
2. `TSE -> VC` 链路在复杂输入上优于 raw -> VC
3. 记录可听风险边界

---

## Phase C：是否值得长期建设
目标：
- 判断是否继续投入
- 决定是否补 enrollment、是否做轻适配、是否做更真实场景训练

里程碑：
1. 用户主观判断通过
2. 自动指标与听感都表明有业务价值
3. 再决定是否继续扩 realism 与训练复杂度

---

# 附录：MVP 最小可实施版本

## MVP 目标
先证明三件事：

1. 现有数据能否合成出对当前任务有信息量的训练样本；
2. TSE baseline 或轻量模型是否能在这些数据上学会“保留用户本人”；
3. TSE 输出是否能作为 VC 前置，在复杂 source 场景下带来净收益。

---

## MVP 最小组件

### 数据侧
- 用户 target speech 池
- 用户 reference 池
- 游戏语音干扰池
- 同学录音 hard negative 池
- 歌曲伴奏干扰池

### 模型侧
- 一条最小 TSE baseline
- 不要求一开始自训复杂结构

### 评估侧
- 单独听感
- 自动分离指标
- `TSE -> VC` 前置链路对照试听

---

## MVP 通过标准

### 标准 1：单独 TSE 可用
在混入其他说话人或音乐的样本上，输出应能明显偏向保留用户本人。

### 标准 2：不严重伤害 VC 输入
TSE 输出送进现有 VC 时，不应普遍劣化。

### 标准 3：复杂场景下有业务收益
在 raw -> VC 明显受干扰影响的输入上，`TSE -> VC` 应至少在一部分样本上有清晰可听改进。

---

# 最终结论

V1.0 的核心判断是：

- 当前用户已有的数据条件，足以启动一个独立的 **Target Speaker Extraction 前置模块** 子项目；
- 首阶段不需要等待完美真实标注录音，也不建议直接并入现有变声模型；
- 最合理路线是：
  1. 使用用户本人 source 作为 pseudo-clean target；
  2. 用游戏语音 / 同学录音 / 音乐构造合成混合数据；
  3. 训练或验证一个独立 TSE 模型；
  4. 先以 `TSE -> VC` 串联链路证明业务价值；
  5. 只有在长期证明有效后，再考虑与 VC 主系统做更深层集成。
